version: '2'
services:
    db:
        image: ilios/mysql-demo
        ports:
            - "17103:3306"
    web:
        environment:
            - ILIOS_REQUIRE_SECURE_CONNECTION=false
        image: ilios/php-apache
        ports:
            - "8000:80"
